# swipe-file-collection-builder

Build categorized swipe-file packs and marketplace-ready listing assets.

## Quick start
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python -m src.run --input examples/swipes.csv --output out
```
